
enum Stuff {
    None,
    Some,
    All
}

enum Stuff2 {
    A(number: int),
    B(str: string),
    C
}

flags

// records are simple type
// mainly for representing data
// they don't have constructors
// they don't have methods
record Stuffy {

}

Main() {
    Directory.GetFiles();
}
